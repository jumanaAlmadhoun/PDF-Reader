<% content_for :body_class, "fullscreen-pdf" %>

<% if @document.pdf_file.attached? %>
  <div class="pdf-fullscreen-layout">
    <div class="pdf-sidebar">
      <div class="pdf-header">
        <h1 class="pdf-title">📚 <%= @document.title %></h1>
        <div class="pdf-meta">
          📎 <%= @document.pdf_file.filename %><br>
          📅 Uploaded <%= time_ago_in_words(@document.created_at) %> ago
        </div>
      </div>
      
      <div class="pdf-actions">
        <div class="nav-links">
          <%= link_to "← Back to Documents", documents_path %>
          <%= link_to "📥 Download PDF", url_for(@document.pdf_file), download: @document.pdf_file.filename %>
        </div>
      </div>
    </div>
    
    <div class="pdf-content">
      <iframe src="<%= url_for(@document.pdf_file) %>"></iframe>
    </div>
  </div>
<% else %>
  <div class="card">
    <div class="card-content">
      <p>No PDF file attached to this document.</p>
      <div class="nav-links">
        <%= link_to "← Back to Documents", documents_path %>
      </div>
    </div>
  </div>
<% end %>